server:
  port: 9099

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: EMPLOYEES-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3200/employeedb
    password: Azerty*-+123
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
